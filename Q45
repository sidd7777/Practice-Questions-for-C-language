#include <stdio.h>

void main() {
    int n;
    printf("Enter a number: \n");
    scanf("%d",&n);
    if (n==0)
        printf("Zero");
    else if (n>0)
        printf("Positive");
    else 
        printf("Negative");
}
