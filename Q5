#include <stdio.h>

int main() {
	int n,c,i=1,sum=0;
	printf("Enter the value of n");
	scanf("%d",&n);
	while(i<=n)
	{
	    printf("Enter number %d:\n",i);
	    scanf("%d",&c);
	    sum+=c;
	    i++;
	}
	printf("Sum = %d",sum);
	return 0;
}
